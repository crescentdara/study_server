<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <!--
        Spring Boot Parent POM
        - 의존성 버전 관리를 자동으로 해줍니다 (버전 충돌 방지)
        - 빌드 플러그인 기본 설정 포함
    -->
    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>3.2.0</version>
        <relativePath/>
    </parent>

    <groupId>com.studyplatform</groupId>
    <artifactId>study-server</artifactId>
    <version>0.0.1-SNAPSHOT</version>
    <name>study-server</name>
    <description>숫자야구 + 빙고 게임 플랫폼 서버</description>

    <properties>
        <java.version>17</java.version>
    </properties>

    <dependencies>
        <!--
            spring-boot-starter-web
            - REST API 구현에 필요한 모든 라이브러리 포함
            - 내장 Tomcat 서버 포함 (별도 서버 설치 불필요)
        -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-web</artifactId>
        </dependency>

        <!--
            spring-boot-starter-websocket
            - WebSocket 지원 (STOMP 프로토콜, SockJS 폴백 포함)
            - spring-messaging 라이브러리 포함
        -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-websocket</artifactId>
        </dependency>

        <!--
            Lombok
            - @Data, @Builder 등 어노테이션으로 보일러플레이트 코드 자동 생성
            - getter/setter/constructor/toString 등을 자동으로 만들어줌
        -->
        <dependency>
            <groupId>org.projectlombok</groupId>
            <artifactId>lombok</artifactId>
            <optional>true</optional>
        </dependency>

        <!-- 테스트 의존성 -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <!--
                Spring Boot Maven Plugin
                - mvn spring-boot:run 으로 실행 가능하게 해줌
                - 실행 가능한 JAR 파일 생성 (mvn package)
            -->
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
                <configuration>
                    <excludes>
                        <exclude>
                            <groupId>org.projectlombok</groupId>
                            <artifactId>lombok</artifactId>
                        </exclude>
                    </excludes>
                </configuration>
            </plugin>
        </plugins>
    </build>
</project>
